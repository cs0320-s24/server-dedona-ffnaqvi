package edu.brown.cs.student.main.Income;

public class IncomeAPIUtilities {

  //    public static List<Soup> deserializeMenu(String jsonList) throws IOException {
  //        List<Soup> menu = new ArrayList<>();
  //        try {
  //            Moshi moshi = new Moshi.Builder().build(); // this is how to initialize moshi
  //            // notice the type and JSONAdapter parameterized type match the return type of the
  // method
  //            // Since List is generic, we shouldn't just pass List.class to the adapter factory.
  //            // Instead, let's be more precise. Java has built-in classes for talking about
  // generic types
  //            // programmatically.
  //            // Building libraries that use them is outside the scope of this class, but we'll
  // follow the
  //            // Moshi docs'
  //            // template by creating a Type object corresponding to List<Ingredient>:
  //            Type listType =
  //                    Types.newParameterizedType(
  //                            List.class, Soup.class); // makes a datatype called type which makes
  // a list of soup
  //            JsonAdapter<List<Soup>> adapter = moshi.adapter(listType);
  //
  //            List<Soup> deserializedMenu = adapter.fromJson(jsonList);
  //
  //            return deserializedMenu;
  //        }

}
